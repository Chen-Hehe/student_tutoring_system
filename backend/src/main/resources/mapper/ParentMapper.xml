<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tutoring.repository.ParentRepository">
    <!-- 根据用户ID查询家长信息 -->
    <select id="findByUserId" resultType="com.tutoring.entity.Parent">
        SELECT * FROM parents WHERE user_id = #{userId} AND deleted = 0
    </select>
    
    <!-- 插入家长信息 -->
    <insert id="insert" parameterType="com.tutoring.entity.Parent">
        INSERT INTO parents (id, user_id, deleted, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{deleted}, #{createdAt}, #{updatedAt})
    </insert>
</mapper>